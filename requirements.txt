# OpenFlo runtime dependencies.
# Pin to the versions known to work together in the .venv this repo ships with.
# Bump intentionally; flow-cytometry math is sensitive to numpy/scipy/sklearn
# major changes.
#
# Install:
#   python -m venv .venv
#   .venv\Scripts\activate                  (Windows)
#   source .venv/bin/activate               (macOS / Linux)
#   pip install -r requirements.txt
#
# Python 3.11+ required (uses contextlib.chdir, PEP 604 unions, typing.Literal).

# ── Flow cytometry I/O & math ─────────────────────────────────────────────────
FlowIO==1.4.0
FlowUtils==1.2.2

# ── Numerical / data ──────────────────────────────────────────────────────────
numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.8.0
openpyxl==3.1.5        # read staining-panel .xlsx (channel↔CD map)

# ── Clustering + dimensionality reduction ─────────────────────────────────────
PhenoGraph==1.5.7
umap-learn==0.5.12
igraph==1.0.0          # phenograph backend
leidenalg==0.11.0      # optional alt community detection
pynndescent==0.6.0     # umap kNN backend
numba==0.65.1          # umap / pynndescent JIT
llvmlite==0.47.0       # numba dep

# ── Plotting ──────────────────────────────────────────────────────────────────
matplotlib==3.10.9
seaborn==0.13.2
pillow==12.2.0         # matplotlib image backend
fonttools==4.63.0
kiwisolver==1.5.0
cycler==0.12.1
contourpy==1.3.3
pyparsing==3.3.2

# ── GUI ───────────────────────────────────────────────────────────────────────
tkinterdnd2==0.4.3     # optional: OS file drag-and-drop into the editor

# ── System / runtime helpers ──────────────────────────────────────────────────
psutil==7.2.2          # memory-aware subsampling decisions
tqdm==4.67.3           # progress bars
joblib==1.5.3
threadpoolctl==3.6.0
python-dateutil==2.9.0.post0
tzdata==2026.2
six==1.17.0
packaging==26.2
colorama==0.4.6        # ANSI colour on Windows terminals
texttable==1.7.0

# ── Optional GPU acceleration (NOT installable from PyPI) ─────────────────────
# RAPIDS provides cudf / cupy / cuml / cugraph for GPU clustering.
# Install via conda:  conda install -c rapidsai -c conda-forge rapids=24.10
# The pipeline auto-detects GPU availability and falls back to CPU.
